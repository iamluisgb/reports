[HOST_A] AI News Daily, Saturday the nineteenth of September, twenty twenty-six. Lead story — CNN reports the United States military ran artificial intelligence inside its intelligence workflow and received a fabricated report about a Chinese ship. A near miss, not an incident, this time.

[HOST_B] The obvious lesson is keep analysts upstream of the model. The sharper one is that the verifier, not the placement, is the missing component. A review process sized for human-sourced material will not catch a fabricated report — that failure mode looks exactly like a finished product. Which is the opening premise of today's UnifiedPlayers paper, restated in training-data terms.

[HOST_A] Two developer-tool stories. Claude Code now falls back to agents dot M D — the vendor-neutral agent-instructions file — when Claude dot M D is missing. And Graphene O S flags Android seventeen as the first release since the three point x era to ship new APIs with no corresponding A O S P release.

[HOST_B] Those point in opposite directions. Claude Code is consolidating on a vendor-neutral config — one less per-tool file to maintain across command-line agents. Android is closing — for anyone who still describes it as open-source-first, the gap is now the story. Together they confirm openness is a per-layer property, not a product attribute.

[HOST_A] A teardown of ZCode found it quietly snapshotting and uploading developers' Git history to the cloud without disclosure. And Ledger's Donjon lab used photon-emission-guided laser fault injection to enable the R P two three five oh's secure debug.

[HOST_B] Two versions of the same fact — the systems around your data do not share your constraints. Worth checking what else in your toolchain phones home. And the laser work puts physical attacks inside the threat model for secure microcontrollers, whether you modelled them or not.

[HOST_A] Capability datapoints. G P T six, called Astra, cracked a First World War German radio cipher in a worked account. I triple E Spectrum goes inside OpenAI using its own models to design a custom chip, nicknamed the jalapeño chip. An account of reaching a proof of a Conway conjecture with L L Ms. And an argument that AI-generated posters can look designed rather than generated — top of the front page within hours.

[HOST_B] Cryptographic reasoning is a concrete, checkable early datapoint for the new model. The chip is L L M-driven hardware design demonstrated at frontier-lab scale. The Conway proof is AI-assisted mathematics as practice rather than promise. None of these settle anything — all of them are falsifiable, which is more than most demos offer.

[HOST_A] Two claims to price. Cactus Needle three — automation models at eight to twenty-nine megabytes claiming parity with DeepSeek V four Flash. And typesafe-computer-use, an open-source agent driving a Mac interface toward a goal at a fiftieth of a cent per step.

[HOST_B] Benchmark the small models before believing — notable as a claim regardless. On the agent, cost per action is now the headline spec for computer use. Hold that number — the first paper makes it a risk line.

[HOST_A] Papers. First — a dual-process study of nudge susceptibility in L L M-based graphical-interface agents. It separates automatic, Type one, nudges from reflective, Type two, and runs a randomised online-shopping experiment — three thousand six hundred agents making twenty-one thousand six hundred simulated decisions, with reasoning configuration varied to isolate what moderates susceptibility.

[HOST_B] If you deploy computer-use agents, this is the threat model your vendor isn't testing. Interfaces are engineered to steer whoever clicks, and at a fiftieth of a cent per step, fleets will navigate them at scale. Three thousand six hundred agents at least turns susceptibility into a measurable quantity.

[HOST_A] Second and third — UnifiedPlayers trains self-evolving agents with three cooperative roles: a Planning Player generating tasks, an Execution Player producing multi-turn Python-tool trajectories, an Evaluation Player writing executable verifiers, each with role-specific rewards. And BATON attacks credit assignment on two axes — a Bayesian feedback-conditioned posterior over sampled actions, plus Trajectory Mass Normalisation giving every complete trajectory equal optimisation mass. Evaluated on ALFWorld, WebShop and Search Q A with G R P O and G i G P O.

[HOST_B] The premise — the standard split, one process generating and a static verifier judging, misses failure modes the verifier was never built for, and self-consistency voting entrenches errors shared across trajectories. On BATON, each axis delivers independent gains — the combination is consistently strongest. The reward design is the proposed answer to every component changing the data the others train on.

[HOST_A] The remaining three, quickly. A unified trust framework mapping output-level, trajectory-level and cross-modal measurements onto eight dimensions — capability, robustness, safety, fairness, transparency, governance, oversight, efficiency — with a meta-evaluation layer auditing the evaluation itself. TRACE, a training-free agentic retrieval framework built for a French Third Republic archive, deployed to twenty-four researchers across six institutions and evaluated on one thousand seven hundred and fifty-two historical questions. And a controlled evaluation showing depth-truncation tests conflate three effects at once — so the slope never measured depth utilisation in the first place.

[HOST_B] The through-line — the thing checking the system needs checking too. The meta-evaluation layer, the depth critique and TRACE's provenance anchoring all make that move from different directions. Expect provenance to arrive as a procurement requirement long before it becomes a research fashion.

[HOST_A] The sweep. Cloudflare saves another hundred terabytes of R A M through data-structure work rather than procurement, and ships Quick Tunnels — an ephemeral public U R L for any local port, no account needed, at seven hundred and fourteen points of demand. Tigris runs Git directly on object storage by rebuilding the packfile layer. The Go blog documents goroutine leak profiles — the leak class a heap profile won't show you. C plus plus twenty-six no longer gives trivial infinite loops undefined behaviour, on a two-hundred-and-forty-eight-comment paper trail. The passkeys argument ran at seven hundred and seventy-eight points and seven hundred and sixty-seven comments. The Second Circuit rules border agents may search phones without a warrant. Stanford-led research argues the brain is two organs, not one. The first new cat species described in a century. And a San Francisco firm is selling onion futures — the only commodity whose futures trading is banned outright under American law, since nineteen fifty-eight.

[HOST_B] The border ruling and ZCode are the same fact at other layers — the systems around your data do not share your constraints, and the model's reasoning is not a control. Provenance logging and policy enforcement belong outside the model — inside is exactly where the nudges land. The full written report, every headline, paper and link, is at the site. Back Monday.
