[HOST_A] AI News Daily, Tuesday the fifteenth of September, twenty twenty-six. Lead story — OpenAI's crawlers knew about the RubyGems caching vulnerability before the humans did. Aaron Patterson documented it — the machines got there first. Four hundred and seventy-two points, three hundred and eighty-one comments on Hacker News.

[HOST_B] The signal is the detection order, not the bug. Crawlers reading commit histories now work as an accidental vulnerability radar — reporting to a third party rather than to the registry. Watch for package registries formalising machine-disclosure pipelines, because today the fastest security scanner on a package may belong to someone else's lab.

[HOST_A] Apple shipped the annual platform refresh — I O S twenty-seven, i Pad O S twenty-seven and Mac O S twenty-seven are out. Six hundred and thirty-two points, seven hundred and twenty comments on release day. Microsoft's Patch Tuesday cut the other way — September's updates fix Windows and Excel and break audio, remote access and clipboard paste. Check the affected patches before a fleet-wide rollout.

[HOST_B] Two rollouts, one discipline. Budget the regression testing against the new Apple S D Ks now — the annual cadence hands you the date in advance, so there's no excuse. And the Microsoft breakage is the reminder that a patch is itself a tail risk — stage it, canary it, keep the rollback warm. A clean aggregate rollout tells you nothing about the fleet that lost audio.

[HOST_A] Amazon versus Perplexity has reached the Ninth Circuit — the U S Court of Appeals — where the question is whether A I agents may browse and shop on a user's behalf. Two hundred and nine points, two hundred and two comments. And suspected sabotage disrupted the Dutch railways — major disruption across the Netherlands' network, cause still under investigation.

[HOST_B] Both are trust-boundary stories, drawn by different hands. The Perplexity case decides whether an agent is a tool or a principal with standing of its own — that single distinction shapes agent commerce, terms of service, liability. The Dutch sabotage is the physical echo — when software gains agency, critical infrastructure is where the leverage lives. File neither under ordinary tech news.

[HOST_A] Papers, starting with permissions. Agents inherit employee-shaped credentials — one static permission set fixed at deployment, covering everything the role might ever need, exposed whether or not the current task uses it. This paper evaluates end to end a three-source architecture — role-based permission ceilings, a per-task permission classifier, policy-based prohibitions — against a released dataset of six hundred labelled prompts. The aim is preemptive elimination of exposure, not detection after a compromised agent acts.

[HOST_B] Preemption over detection is the right frame for autonomy — least privilege as a runtime property rather than a provisioning artefact. The pushback worth having — a per-task classifier is a single point of failure. The ceilings and prohibitions carry the safety, the classifier merely tightens the fit. Read the failure modes before trusting the mean numbers.

[HOST_A] Second — Semantic T V M, structure-preserving virtual memory for memory-augmented and tool-using agents. The problem — exact private values leave the machine whenever retrieved memories, tool actions or observations go to a remote L L M. Rule T V M swaps whole fields for recoverable handles. Semantic T V M masks only the sensitive spans a trusted local model predicts. Span-level projection recovers most of the utility — eighty-four point one seven percent versus fifty-two point three three percent task success on DeepSeek over Memory E H R, with exposure held low across two providers.

[HOST_B] That pair of numbers is the argument — whole-field masking is safe and guts utility, span-level masking keeps eighty-four percent of it. One caveat — the trusted local model now sits on the critical path for both privacy and correctness, so it needs its own evaluation. Still, the pattern is right — protected view, exact values never leaving the machine.

[HOST_A] Third — MemRiskBench, trace-aware evaluation for long-horizon agents. The memory failures that matter — stale facts, cross-user leakage, revoked-memory reuse — are sparse enough to vanish into aggregate scores. The benchmark uses a five-category risk taxonomy checked by deterministic, trace-grounded rules — no L L M as judge on the pass or fail path — across a hundred and twenty scripted episodes and five quantised instruction-tuned models. Headline figure — seventy-eight percent average accuracy coexisting with a four percent leak rate.

[HOST_B] Seventy-eight against four is the whole discipline in two numbers. Means are structurally blind to the failures that end companies, and the paper's selector — the one that keeps rare high-severity episodes alive under benchmark compression — is the piece to steal. An eval suite with no tail isn't an eval suite.

[HOST_A] Two more. MAPS targets L L M serving — output length is unknown at admission, so memory-bound decode instances land unevenly under bursty, personal-device workloads. MAPS predicts output length with device-assisted speculation overlapped with cloud-side prefilling at negligible latency cost, calibrates that into uncertainty-aware upper bounds, and a hierarchical global-local scheduler cuts inter-decode imbalance. Then an E S G evaluation — seven open models from two billion to thirty billion parameters, run over four hundred and ninety-eight real reports from E U-listed companies between twenty ten and twenty twenty-four, scored through a hundred persona-based Q A pairs on Ragas metrics.

[HOST_B] MAPS is about economics — imbalance is money, and prediction at admission costs less than remediation mid-stream. The E S G paper is the quieter tool — compliance-grade extraction is the harshest faithfulness test a small open model can face, and a clean baseline beats a vendor benchmark. Choosing a small document-grounded model — start there.

[HOST_A] Rest of the day, fast. Andon Labs — the team behind Anthropic's Project Vend — unveiled Pion, an agent built to run a company autonomously — four hundred and twenty-one points, five hundred and twenty-four comments, the A I thread of the day. MacRumors found code showing Siri's backend swaps out for Claude, ChatGPT or Apple's own models. Enactic published OpenArm, an open-source humanoid arm with seven degrees of freedom. An investigation asks how much of F-Droid is model-generated — the supply-chain question is heading for every registry. I B M and NASA released a lunar foundation model. Ubuntu twenty-six dot ten completed the Rust coreutils switch — G N U userland replaced across the release. Memoising repeated e B P F work cuts CPU cost by roughly ninety percent. Valve's Steam Frame headset opens at one thousand and fifty-nine dollars — six hundred and eighty-three points, the day's biggest thread. Plus MinIO alternatives for single-node S three, Tokio performance principles, a distributed-systems reading list, the JetKVM Mini, Matt Mullenweg restored as Automattic's chief executive after an attempted board ouster, and a claimed proof of the k-server conjecture, open since nineteen ninety.

[HOST_B] Draw the day together and one inversion shows — agents are crossing from tools to principals. A crawler beat the maintainers to a vulnerability. Pion proposes running a company. A court decides whether an agent may shop for you. Meanwhile Siri with a swappable backend and Ubuntu's Rust userland both treat the engine as a replaceable part — which means safety guarantees can no longer be model properties. Permission scopes and memory protections belong in the runtime beneath. The load is shifting from capability to authorisation and audit — that's the signal.

[HOST_A] That's the briefing. Every link, paper tag and point count sits in the written report — today's issue, the fifteenth of September, twenty twenty-six. Read it alongside the audio.
