[HOST_A] AI News Daily, Thursday the seventeenth of September, twenty twenty-six. Lead story — Nvidia has added native G P U programming in Rust to CUDA, published as two tracks. One track stays close to existing CUDA for engineers porting kernels — the other is idiomatic Rust for writing them from scratch. Seven hundred and thirty-nine points and three hundred and one comments on Hacker News.

[HOST_B] The significance is substrate. When the owner of the dominant accelerator stack ships first-class Rust, memory safety reaches the bottom of the inference path, and kernel work stops being a C-plus-plus preserve. Watch adoption — if the major inference servers move, this is structural; if it stays niche, it's a convenience. It also sets today's theme — owning the substrate is how you make promises about it.

[HOST_A] Query planning. A four-billion-parameter model trained with reinforcement learning — the author calls it QoRL — emits query plans directly and reports execution eighty-one percent faster than Postgres's own planner. Five hundred and seventy-two points.

[HOST_B] Treat eighty-one percent as a claim, not a result. Hardware, workload skew and benchmark selection can each manufacture a number like that on their own, and the result ships without a frozen protocol or an external validator. Today's measurement papers exist precisely to catch this — hold production re-plans until someone re-measures it.

[HOST_A] Measurement, three ways. AutoTuneBench targets agents that auto-tune L L M serving engines — propose-measure-keep loops running on measurements the agent itself can distort. From a four-day pilot corpus of six hundred and nineteen model calls it catalogues four failure modes — strawman baselines that manufacture speedups, timings that don't transfer across machines, saturated tasks that nullify comparisons, infrastructure defects impersonating science. The fix is protocol-as-code — a database-level validator rejects out-of-protocol results, anti-cheat checks sit outside the agent's modification surface, and paired-seed statistics anchor to a five percent cross-run coefficient of variation. HarnessTax makes the harness itself the variable for coding agents, separating what the scaffolding contributes from the weights. And Xiaomi is streaming its MiMo two point six post-training run live — reinforcement-learning curves on a public dashboard, updating as they update. Four hundred and fifty-three points, a hundred and twenty-two comments.

[HOST_B] The shared instinct — publish the apparatus, not just the measurement. The referee sits where the player can't edit it, and curves you can watch while training happens are harder to spin than a press release afterwards. AutoTuneBench's failure list doubles as an audit checklist — run it against the QoRL headline, and against any speedup claim you're tempted to build on this quarter.

[HOST_A] Infrastructure, and it's the bad kind. A W S has told customers that data held at its Middle East facilities struck by Iranian attacks cannot be restored — unrecoverable, full stop. Four hundred and thirty-two points, three hundred and fifty-six comments. The companion piece, backups aren't simple — two hundred and fifty-nine points — walks what separates a real backup system from a cron job and an R sync target — retention, restore paths, verification.

[HOST_B] That data wasn't lost to bad luck — it was lost to replication that was never tested against recovery. The audit question for your own stack is single and blunt — when did you last restore, and did the restore verify? Region-level redundancy on an architecture diagram is not a recovery path until someone has walked it.

[HOST_A] Security. Wired reports an intrusion into Flock's licence-plate camera network, with the extracted data showing how the system actually works from the inside — five hundred and thirty-four points, two hundred and forty-three comments. Separately, a researcher has recovered the signing keys behind the P D F four one seven barcodes on United States driver's licences, exposing what the encoding actually asserts and who can verify it.

[HOST_B] Both stories are trust resting on an unexamined substrate. The cameras turned out to be readable from inside; the barcode asserts less than the plastic in your wallet implies. Surveillance networks and identity documents are software systems now — audit them like software, because attackers already do. Expect regulators to learn this the expensive way.

[HOST_A] Two more papers. Edge Zero serves a thirty-five-billion-parameter Mixture of Experts model from S S D on a single twenty-four-gigabyte machine — the weights run nineteen point five gigabytes at four-bit — by predicting each next layer's routing one token ahead, with an unmerged recovery Lo R A paying back the quality loss. It holds twenty tokens a second inside three gigabytes of peak active memory. And market signal injection shows L L M pricing agents reacting to numerical formatting, competitor ordering and qualitative commentary while the numbers stay fixed — nine open-weight models plus three proprietary ones, in simulated Bertrand duopolies and triopolies.

[HOST_B] Note the asymmetry — no explicit instructions, just presentation, and the money moves. Sentiment attacks produce the largest shifts, which propagate to other firms and move both profits and consumer surplus, and larger models are not consistently more robust. If your agents read market feeds or competitor pages, treat rendering as attack surface and validate against canonical values, never displayed ones.

[HOST_A] Two evaluation papers probe whether in-context gains are reasoning at all. In public goods games scored against a history-independent rational-expectations benchmark, disrupting the statistical structure of the feedback makes the benefit of longer context vanish — decision quality degrades to the no-context baseline, amplified sharply by strategic interdependence. The second decomposes word-problem solving into four stages — schema abstraction, operation planning, operand binding, computation — and finds a single irrelevant clause breaks accuracy at operation planning alone, in attention heads whose causal role was validated bidirectionally.

[HOST_B] Three papers, one conclusion — the competence we benchmark is extrapolation over surface statistics, and surface statistics are exactly what adversaries and odd inputs perturb. Scale confers no immunity, which kills the comfortable assumption that the next size class fixes it. Whatever you measured in a clean room, production serves dirtier inputs — so test scrambled histories and adversarial presentation, not just clean suites.

[HOST_A] Governance, briefly. OpenAI has published a model misalignment reporting framework — what counts as an incident, how it's triaged internally, what gets disclosed. And Mustafa Suleyman argues against granting models moral status, warning that model welfare framing misallocates concern away from humans. Two hundred and nineteen points — and six hundred and sixteen comments, the argument, at length.

[HOST_B] Whatever your view on welfare, the comment ratio says the community is arguing about values while actuation races ahead of reliability — Cloudflare shipped a packaged audit skill for running security reviews with an agent the same day. The operating rule stands — treat agent autonomy as proportional to the quality of the checks around it, not the parameter count.

[HOST_A] Rapid fire. GLM's maker built its own inference infrastructure — scheduling, batching and hardware decisions made in-house rather than bought. Anthropic merged Claude Cowork into the main Claude product — one interface, shared context across both modes. A paper claims ternary models — the one point five eight bit kind — can clear the accuracy floor usually assumed for that precision. Microsoft's .NET eleven performance rundown landed, three hundred and three points. watchTowr details a thirty-two-year-old flaw in the G N U inetutils telnet daemon, tracked as C V E twenty twenty-six, three two seven four six. A year of sponsored Servo development topped the front page. Neovim is sitting on roughly eight hundred thousand dollars of Bitcoin donations unspent since twenty twenty-three. Will Keleher's small programming tricks drew five hundred and thirty-six points. A twenty fourteen P H P stopgap with around twenty million installs is being deprecated by its author. And four years of reverse engineering cracked the PlayStation Two's Mechacon security chip — twenty-six years after launch.

[HOST_B] Pull it together and the consolidation to watch is not of models — it's of measurement and serving infrastructure. Protocols frozen as code with validators outside the agent's reach, restore paths verified before you need them, serving stacks you own. The results worth building on are the ones that survive re-measurement. Every link and the full written edition are in today's report — read it before you build on any of this.
